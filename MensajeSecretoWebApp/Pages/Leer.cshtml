@page 
@model LeerModel // Especifica el modelo de página que se usará para la lógica y los datos.
@{ 
    ViewData["Title"] = "Leer Mensaje"; // Asigna el título de la página para la vista.
}

<h2>📤 Leer mensaje recibido</h2> 

<form method="post" enctype="multipart/form-data"> @* Inicia un formulario HTML que enviará los datos por método POST y permite subir archivos.*@
    <div class="mb-3">
        <label for="archivo" class="form-label">Selecciona el archivo XML recibido:</label> @* Etiqueta para el campo de selección de archivo.*@
        <input type="file" class="form-control" id="archivo" name="ArchivoXml" accept=".xml" required /> @* Campo para seleccionar el archivo XML, obligatorio.*@
    </div>
    <button type="submit" class="btn btn-primary">Leer mensaje</button> 
</form>

@if (Model.MensajeDescifrado != null) // Si el mensaje descifrado no es nulo...
{
    <div class="alert alert-success mt-3"> @* Muestra un cuadro de alerta de éxito.*@
        <strong>Mensaje descifrado:</strong>
        <p>@Model.MensajeDescifrado</p> @*Muestra el mensaje descifrado.*@
        <hr />
        <strong>Remitente:</strong> @Model.Remitente <br /> @* Muestra el remitente.*@
        <strong>Código:</strong> @Model.Codigo <br /> @* Muestra el código.*@
        <strong>Fecha:</strong> @Model.Fecha @* Muestra la fecha.*@
    </div>
}